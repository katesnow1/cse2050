def generic_hi(name = "world"):
    """This function returns "Hello, " along with the parameter inputted, in case of no parameter, there is a default value of "world" """
    return "Hello, " + name + "!"
